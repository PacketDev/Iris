const host = 'https://localhost:7070';
export const registerEndpoint = `${host}/auth/v1/register`;
export const loginEndpoint = `${host}/auth/v1/login`;
export const avatarEndpoint = `${host}/setAvatar`;
